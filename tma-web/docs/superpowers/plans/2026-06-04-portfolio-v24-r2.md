# Portfolio V24 — Revision 2 (R2) Plan

> Execute task-by-task via subagent-driven-development. Builds on the shipped V24.

**Goal:** Rework V24 per user feedback: (1) galaxy/nebula animated background using the slide-7 palette, (2) Foodics & Zid become their own expandable film sections (7 / 2 films) with click-to-fullscreen, (3) the rest grouped by category as a premium **asymmetric editorial film grid** with autoplay video + click-to-fullscreen, (4) new hero copy, (5) more premium/advanced grid styling.

**Approach:** Reuse the proven V23 mechanics (featured film block, shared `openFilm` lightbox, dual-layer hover, lazy autoplay) but ported to V24's namespace + galaxy aesthetic, keeping V24 self-contained. All motion respects reduced-motion; videos lazy-autoplay and pause offscreen.

**Slide-7 galaxy palette (sampled hex):** blue `#184DE2`, indigo `#5666C6`, deep-indigo `#4D45AE`, violet `#6A4193`, purple `#743D8D`, magenta `#903877`, crimson `#C43945`, red `#E94021`, orange `#E87440`, ocean `#2667A9`, teal `#1A7B84`, tan `#DDAB84`.

**Key existing references (read these):**
- `components/portfolio-v22/showreel.js` — `SHOWREEL` (foodics 7 films, zid 2 films; each film `{id,title,group,kind:'youtube'|'mp4',youtubeId?,src?,poster}`).
- `components/portfolio-v20/projects.js` — `PROJECTS`; 10 have a `video` field (lsc-vision-2034, vodafone-global, rubicon-exotic, linc-card, zaintech-drones, investcorp-capital, mawani-vision-2030, world-government-summit, family-development-finance, sharjah-data-smart-gov).
- `components/portfolio-v23/V23Featured.jsx` + `useV23Lightbox.js` + `V23VideoLightbox.jsx` + `V23Cursor.jsx` — the featured-block, lightbox store, portal, and `data-cursor` magnetic cursor to port/reuse.
- `components/portfolio-v23/projects.js` `buildFeatured()` — how `SHOWREEL` films map to `media[]` with `span`/`ratio` rhythm.

---

## Task R1 — data layer: galaxy palette, FEATURED (Foodics/Zid), video-aware media, taxonomy excludes featured

**Files:** Modify `components/portfolio-v24/data.js`; Modify `test/portfolio-v24-data.test.mjs`.

Changes to `data.js`:
1. Add exported `GALAXY_STOPS` = the 12 slide-7 hex above (ordered blue→indigo→violet→magenta→crimson→red→orange→ocean→teal→tan for a nebula spread). Keep existing `AGENCY_STATS`, `statsFor`, `descFor`, `worksFor`, `CATEGORIES`, `PROJECTS`, `PROJECTS_BY_SLUG`.
2. Add `FEATURED_SLUGS = ["foodics-boundless","zid-ripple"]` and build `export const FEATURED` from `SHOWREEL` + `PROJECTS_BY_SLUG`, mirroring `portfolio-v23/projects.js buildFeatured()`: each entry `{ slug, client, title, tagline, intro, results, media }` where `media` maps each film to `{ kind, src?, youtubeId?, href?, poster, title, group, span, ratio }` using a hand-tuned layout rhythm (reuse the V23 `FEATURED_LAYOUT` spans/ratios for foodics-boundless and zid-ripple). Import `SHOWREEL` from `../portfolio-v22/showreel.js`.
3. Update `mediaFor(project)`: if `project.video` → `{type:"video", src: project.video, poster: project.hero||project.thumb}`; else `{type:"image", src: project.hero||project.thumb}`. (Drop the SHOWREEL cardVideo path — featured projects no longer go through the grid.)
4. Add `export function hasVideo(project) { return !!project.video; }`.
5. Taxonomy: remove Foodics & Zid from the category buckets. Change `EXPLICIT.events` removal — delete the `events` bucket entirely (it only held foodics/zid). Final `ORDER`/`META` = `["brand","social","film","digital"]`. Add `foodics-boundless` & `zid-ripple` to a `FEATURED_SLUGS` set and have `worksFor`/`BUCKET_OF` skip them so they never appear in a category grid. Keep the "no project dropped" guarantee for the OTHER 27.

Tests to update (`test/portfolio-v24-data.test.mjs`):
- `FEATURED` has 2 entries; foodics media length 7, zid media length 2; every media item has a `poster` and either `src` or `youtubeId`.
- `GALAXY_STOPS` length ≥ 10, all `#`-hex.
- The 27 non-featured projects each land in exactly one of the 4 categories (no foodics/zid in any category; no other drops, no dupes).
- `mediaFor`: a video project (e.g. `rubicon-exotic`) → `{type:"video"}` with `.src` ending in the project's video; an image-only project (e.g. `sol-brand`) → `{type:"image"}`.
- Keep the existing statsFor tests (real KPIs for foodics still resolve via `FEATURED`/`statsFor`).

Run `node --test test/portfolio-v24-data.test.mjs` → all pass. Commit `feat(v24-r2): data — galaxy palette, FEATURED foodics/zid, video-aware media`.

---

## Task R2 — galaxy/nebula animated background

**File:** Modify `components/portfolio-v24/V24GradientField.jsx` (+ minor `v24.css` if needed).

Rework into a "swimming in galaxy" field on black, using `GALAXY_STOPS` from `./data`:
- **Nebula layer:** 4–5 large blurred radial clouds in galaxy hues, each drifting on its own slow sine/cos path (independent phase) so they swim and overlap; subtle scale breathing.
- **Scroll coupling:** read normalized scroll AND scroll velocity; velocity adds a gentle drift "current" (swim faster while scrolling), position offsets with scroll progress so the galaxy travels as you move down.
- **Starfield:** a sparse field of small points (50–120) drifting slowly with subtle parallax + twinkle (opacity flicker), screen-blended — gives the galaxy depth. Keep cheap (transform/alpha only).
- Timestamp-driven rAF (frame-rate independent), `dpr` capped at 2, pause on `visibilitychange`, single static frame under `prefers-reduced-motion`. Screen blend over black, behind content (`.v24-gradient-field` already fixed z0). Keep the vignette.

Commit `feat(v24-r2): galaxy nebula background — drifting clouds + starfield, scroll-swim`.

---

## Task R3 — video lightbox (click-to-fullscreen)

**Files:** Create `components/portfolio-v24/useV24Lightbox.js`, `components/portfolio-v24/V24VideoLightbox.jsx`; add styles to `v24.css`.

Port `useV23Lightbox.js` → `useV24Lightbox.js` verbatim (shared external store: `openFilm`, `closeFilm`, `useV24Lightbox`), exposing `window.__v24OpenFilm`. Port `V23VideoLightbox.jsx` → `V24VideoLightbox.jsx` with `.v24-film*` classes: a portal dialog with scrim (click-to-close), close button, ESC + body scroll-lock; renders a youtube `<iframe ...autoplay=1>` for `kind:"youtube"` else `<video controls autoPlay playsInline>`; optional title/client caption. Add `.v24-film`, `.v24-film-scrim`, `.v24-film-frame`, `.v24-film-close`, `.v24-film-media` (16/9), `.v24-film-title` styles to `v24.css` (dark scrim + backdrop-blur, centered frame rise-in). Commit `feat(v24-r2): video lightbox — click a film to play fullscreen`.

---

## Task R4 — Foodics & Zid featured film sections

**Files:** Create `components/portfolio-v24/V24Featured.jsx`; add styles to `v24.css`.

Model on `V23Featured.jsx`, scoped to `FEATURED` from `./data`. For each featured project render a `.v24-feat` block:
- Header: eyebrow `"{client} — Featured Case"`, title `"{title}. {tagline}."` (line-reveal via `useV24Reveal`), intro paragraph, and a `.v24-feat-stats` list of `results.slice(0,4)` ({metric,label}).
- **Lead film** (first `kind:"video"` else first): autoplay muted loop video (iris reveal), caption (title/group), click → `openFilm`.
- **"View all N films"** magnetic button toggles a measured-height expand panel (`expand-panel-height-auto`) revealing the **rest of the films** in an asymmetric grid (`.v24-el-{span}` with each film's `ratio`); on expand, iris-cascade the revealed `.v24-im` via a `--mask` GSAP stagger (`featured-reveal-cascade-stagger`). Each film cell: lazy autoplay muted video (youtube → poster image), `data-cursor="blob" data-cursor-label="Play"`, click → `openFilm({kind,src|youtubeId,poster,title,client})`. After expand, `ScrollTrigger.refresh()`.
- Use `useV24Reveal`; reuse the V23 lazy-autoplay approach (IntersectionObserver swapping `data-src`→`src`) or simply autoplay with `preload="metadata"` for the lead and lazy for the rest.
Add `.v24-feat*`, `.v24-el*`, `.v24-im`/`.v24-im-h`/`.v24-im-hv` dual-layer, `.v24-play` badge, `.v24-more-bt`, `.v24-feat-rest` expand styles to `v24.css` (galaxy-premium treatment, not a literal v23 blue copy). Commit `feat(v24-r2): Foodics & Zid featured film sections with expand + lightbox`.

---

## Task R5 — asymmetric editorial work grid (the rest, premium)

**Files:** Rework `components/portfolio-v24/V24CategoryGroup.jsx` + `V24Categories.jsx`; delete/replace `V24WorkRow.jsx` usage; add styles to `v24.css`. (Keep `V24Media.jsx` or inline.)

Per category (4 groups, from `worksFor`), render an **asymmetric editorial grid** of premium cards (varied `span`/`ratio` rhythm like V23 `WORK_GRID`):
- Each card: if `hasVideo(project)` → autoplay muted loop `<video>` (lazy/offscreen-pause) with a `.v24-play` badge and `data-cursor="blob" data-cursor-label="Play"`, click → `openFilm({kind:"video", src: project.video, poster, title, client})`; else still `<img>` (ken-burns), no lightbox (click is inert or opens nothing).
- **Premium hover:** dual-layer media (`.v24-im-h`/`.v24-im-hv` crossfade), `:has()` spotlight that dims/desaturates non-hovered siblings (`featured-grid-spotlight-dim-siblings`), card lift + poster zoom.
- **Stats/description:** show client + title + category always; reveal the `statsFor` chips + `descFor` text on hover via an overlay/scrim (or a always-visible compact meta row beneath). Keep it readable over the galaxy bg.
- Reveal each card with `useV24Reveal` (bidirectional fade kept).
- Category header (`useV24Reveal`) with label + zero-padded count (count of that category's works).
Add `.v24-wgrid`, `.v24-wcard`, dual-layer hover, spotlight `:has`, hover meta overlay styles to `v24.css`. Commit `feat(v24-r2): asymmetric editorial work grid with premium hover + lightbox`.

---

## Task R6 — new hero copy + page wiring

**Files:** Modify `components/portfolio-v24/V24Hero.jsx`; modify `app/portfolio-v24/page.jsx`.

- Hero: replace eyebrow + title with new authentic brand copy from the guidelines: eyebrow `"The Motion Agency — Riyadh · Amman"`, title `"We don't just design — we bring brands into motion."` (sourced from Brand Guidelines slide 3). Keep the hero media video + reveals.
- `page.jsx`: import & mount `V24VideoLightbox` (near the top, like V23) and `V24Featured` **between** `V24StatsBand` and `V24Categories`. Order: Preloader, GradientField, SmoothScroll, V23Cursor, V24VideoLightbox, ClientShell, Nav, Hero, StatsBand, **Featured**, Categories, Contact, Footer.
Commit `feat(v24-r2): new hero copy + wire featured sections and lightbox`.

---

## Task R7 — e2e update + production verification

**Files:** Modify `playwright/tests/v24.spec.js`.

Update/extend tests (skip preloader for content tests):
- Hero H1 contains `/motion/i` (new copy).
- Two `.v24-feat` featured sections render; the first contains "Foodics" and a lead `video`; a `.v24-feat-stats` shows a real KPI (35.6%/32,000+/$1B); clicking a featured film cell opens `.v24-film` (lightbox) and ESC/scrim closes it.
- "View all N films" button exists on Foodics and expands (`.v24-feat-rest` becomes visible / more `.v24-im` shown).
- Category groups render for the rest (4 groups, ≥ ~20 cards total across them, no Foodics/Zid card in a category grid).
- At least one work card is a `<video>` (autoplay) and clicking it opens `.v24-film`.
- Gradient field canvas mounts; reduced-motion smoke still fine.
Then: production `npm run build` passes; run full e2e across the 6 viewport projects; `node --test test/portfolio-v24-data.test.mjs` passes. Fix any real integration bugs. Commit `test(v24-r2): e2e for featured sections, lightbox, premium grid` (+ any `fix(v24-r2): …`).

---

## Notes
- Do NOT modify V20–V23. Reuse their data; port their mechanics into v24-namespaced files.
- Real numbers only (Foodics/Zid KPIs real; others factual meta).
- Keep reduced-motion + offscreen-pause + cleanup discipline throughout.
